#ifndef PROCESS_CD_COMMAND
#define PROCESS_CD_COMMAND

// This function move the location of the user through the directories when he uses cd
// and update the tracking of location directory
char *process_cd_command(char **words);

#endif